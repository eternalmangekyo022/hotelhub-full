{
	"dependencies": {
		"concurrently": "^9.1.2"
	},
	"scripts": {
		"dev": "concurrently \"cd backend && bun dev\" \"cd frontend && bun dev\"",
		"ins": "concurrently \"cd backend && bun install\" \"cd frontend && bun install\""
	}
}